<li>
  <span class="user">
    <%= link_to account_item.name, account_item.name %>
    <% if current_user?(account_item.user) %>
      <%= link_to "delete", account_item, method: :delete, data: { confirm: "You sure?" } %>
    <% end %>
  </span>

</li>
