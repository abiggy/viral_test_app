<%= form_for(@new_follower) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :name, "Twitter username:" %>
  <%= f.text_field :name %>

  <%= f.submit "Add Twitter account", class: "btn btn-large btn-primary" %>
<% end %>
